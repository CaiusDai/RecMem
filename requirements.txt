# Core dependencies
numpy>=1.24.0
pandas>=2.0.0

# OpenAI and LLM related
openai>=1.0.0
tiktoken>=0.5.0
json-repair>=0.30.0

# Vector search
qdrant-client>=1.15.0

# Evaluation metrics — only BLEU (via nltk) is used in the default benchmark
# pipeline. ROUGE / BERTScore / METEOR / sentence-similarity are available
# from `evaluation.metrics.utils` but require optional extras; see
# `requirements-extra-metrics.txt`.
nltk>=3.8.0

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0
jinja2>=3.1.0
